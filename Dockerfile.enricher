FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir \
    psycopg2-binary \
    requests \
    rapidfuzz

COPY . .

CMD ["python", "-m", "collectors.spotify_enricher"]
